{
  "name": "@ajmal_n/lumen-cli",
  "version": "0.3.0",
  "description": "Scan any repo and emit a self-contained HTML or Markdown report — with test-coverage breakdown for Jest, Vitest, Nx, Jasmine, Karma, Mocha, and more.",
  "license": "MIT",
  "bin": {
    "lumen": "dist/index.js"
  },
  "main": "dist/index.js",
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "dependencies": {
    "@ajmal_n/lumen-core": "0.3.0",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  }
}
